# Aggiornare le credenze su un parametro: dalla prior al posterior {#sec-bayesian-inference-proportion}

## Introduzione {.unnumbered .unlisted}

Nei capitoli precedenti abbiamo esaminato il ruolo fondamentale dell'incertezza nella ricerca psicologica e abbiamo introdotto l'approccio bayesiano come framework per la sua rappresentazione formale. Questo capitolo è dedicato all'analisi di un caso di studio particolarmente ricorrente e formativo: l'inferenza sulla proporzione di successi in contesti sperimentali. Illustreremo il processo attraverso il quale le credenze iniziali -- formalizzate nella *distribuzione a priori* -- vengono aggiornate alla luce dell'evidenza empirica, generando la *distribuzione a posteriori*.

### La rilevanza delle proporzioni in psicologia {.unnumbered .unlisted}

Una parte consistente dei dati in psicologia assume forma binaria. Si considerino, ad esempio, fenomeni come il richiamo o il mancato richiamo di uno stimolo in un compito di memoria, la remissione o la persistenza di un sintomo in ambito clinico o l’accuratezza di una risposta in un paradigma decisionale. In tutti questi scenari, la variabile osservata è essenzialmente una sequenza di successi e insuccessi, e l'obiettivo inferenziale consiste nello stimare la proporzione sottostante nella popolazione di riferimento.

L'analisi di questo caso apparentemente semplice è fondamentale: permette di stabilire una connessione diretta tra i dati osservati e i modelli probabilistici, di comprendere il funzionamento operativo del modello Beta-Binomiale nell'aggiornamento delle credenze e di gettare le basi per estensioni verso modelli più complessi che possano catturare la dinamica dei processi psicologici che generano i dati.

### Prospettiva bayesiana e replicabilità {.unnumbered .unlisted}

Il dibattito sulla crisi della replicabilità ha evidenziato ripetutamente criticità metodologiche negli studi psicologici che confrontano proporzioni tra condizioni sperimentali. Una limitazione metodologica frequentemente rilevata è la presentazione dei risultati sotto forma di stime puntuali, senza un'adeguata quantificazione dell'intervallo di incertezza associato a tali stime. Questa pratica ostacola una valutazione realistica della precisione delle stime e della solidità delle conclusioni.

L'approccio bayesiano fornisce una risposta a questa limitazione. Oltre a identificare i valori più plausibili per il parametro di interesse, esso quantifica esplicitamente l'incertezza residua lungo l'intera distribuzione a posteriori. Questa trasparenza metodologica promuove un'interpretazione più cauta e cumulativa dei risultati, contribuendo ad affrontare alcune delle criticità emerse nel dibattito sulla replicabilità nella ricerca psicologica.

### Panoramica del capitolo {.unnumbered .unlisted}

- Applicare l'aggiornamento bayesiano per affinare credenze.
- Rappresentare distribuzioni a priori (discrete e continue).
- Calcolare la verosimiglianza e aggiornare la distribuzione a priori.
- Derivare e interpretare la distribuzione a posteriori.
- Usare il metodo a griglia per approssimare la distribuzione a posteriori.
- Applicare il modello binomiale per stimare probabilità e incertezze.
- Calcolare medie, mode e intervalli di credibilità.
- Utilizzare la distribuzione Beta come prior continuo.

::: {.callout-tip collapse=true}
## Prerequisiti

- Leggere il settimo capitolo del testo di @albert_2019prob.
:::

::: {.callout-caution collapse=true title="Preparazione del Notebook"}

```{r}
#| output: false
here::here("code", "_common.R") |> 
  source()

# Load packages
if (!requireNamespace("pacman")) install.packages("pacman")
pacman::p_load(HDInterval)
```
:::

## Verosimiglianza binomiale

Nei capitoli precedenti abbiamo introdotto l’idea centrale dell’approccio bayesiano: l’incertezza non si rappresenta con un singolo numero, ma con una distribuzione di probabilità. Possiamo ora applicare questo principio a un caso concreto e molto frequente nella ricerca psicologica: l’osservazione di una sequenza di successi e insuccessi.

Dati di questo tipo sono molto comuni. Si pensi, ad esempio, a uno studente che risponde a un test (risposta corretta/risposta errata), a un paziente che manifesta o meno un miglioramento sintomatologico o a un partecipante che, in un compito decisionale, sceglie o non sceglie uno stimolo specifico. In tutti questi casi, la variabile osservata è un conteggio di successi su un numero predeterminato di prove. La struttura probabilistica che descrive matematicamente tali conteggi è catturata dalla distribuzione binomiale, che funge da modello fenomenologico di riferimento per i dati di tipo binario.

La distribuzione binomiale specifica la probabilità di osservare esattamente $y$ successi in $n$ prove indipendenti, in cui ciascuna prova ha una probabilità di successo pari a $\theta$:

$$
p(y \mid \theta) \;=\; \text{Bin}(y \mid n, \theta) \;=\; \binom{n}{y} \, \theta^y (1 - \theta)^{n-y},
$$
dove $\theta$ rappresenta la probabilità di successo per prova, $y$ è il numero di successi osservati, e $n$ è il numero totale di prove, fissato in anticipo.

## Esempio psicologico: giudizio morale in un dilemma

Un ambito di applicazione è lo studio dei dilemmi morali, come il celebre *problema del treno* [@foot1967problem; @greene2001fmri], ampiamente utilizzato in psicologia per studiare i processi decisionali in contesti etici [si veda, ad esempio, @bucciarelli2008psychology]. Supponiamo che un ricercatore voglia stimare la proporzione di adulti che giudicano moralmente accettabile un'azione controversa, come deviare il percorso di un treno per salvare cinque persone, consapevoli che ciò causerà la morte di un'altra persona.

Ogni partecipante, dopo aver letto la descrizione dello scenario, fornisce una risposta dicotomica:

- 1 = giudica l'azione moralmente accettabile (successo),
- 0 = giudica l'azione non accettabile (insuccesso).

Ipotizziamo che in un campione di 30 soggetti indipendenti, 22 abbiano espresso un giudizio favorevole.

La formalizzazione probabilistica di questo disegno sperimentale è la seguente:

- ciascun partecipante fornisce un giudizio indipendente, modellabile come una prova di Bernoulli con parametro $\theta$;
- il conteggio totale dei giudizi favorevoli segue una distribuzione binomiale:

$$
Y \sim \text{Binomiale}(n = 30, \theta),
$$
dove $Y = 22$ rappresenta il numero di successi osservati nel campione.

### Obiettivo inferenziale

L’obiettivo inferenziale consiste nella stima di $\theta$, ovvero la probabilità che un adulto, estratto casualmente dalla popolazione di riferimento, giudichi moralmente accettabile l’azione proposta nel dilemma.

All'interno del quadro bayesiano, tale stima emerge dalla sintesi di due fonti di informazione: la verosimiglianza, basata sull'evidenza empirica (22 successi su 30 prove), e la distribuzione a priori, che formalizza le credenze iniziali sul parametro $\theta$. La combinazione di questi due elementi genera la distribuzione a posteriori, che rappresenta in modo esaustivo il nostro stato di conoscenza aggiornato sul parametro, condizionato all’osservazione dei dati.

### Metodo basato su griglia

Il metodo basato su griglia rappresenta un approccio intuitivo e istruttivo per comprendere il meccanismo dell'aggiornamento bayesiano. La sua logica fondamentale consiste nel suddividere lo spazio dei parametri in intervalli discreti e nel calcolare la distribuzione a posteriori in modo puntuale, evitando l'uso di metodi analitici complessi o tecniche di campionamento stocastico.

La procedura si articola in quattro fasi essenziali:

1.  **definizione della griglia:** si seleziona un insieme finito di valori plausibili per il parametro di interesse ${\theta_1,\dots,\theta_K}$, ad esempio, da 0 a 1 a passi di 0.1;
2.  **calcolo della verosimiglianza:** per ogni valore $\theta_i$ nella griglia, della griglia, si valuta quanto i dati osservati siano compatibili con quel valore specifico del parametro, $L(\theta_k)=p(y\mid \theta_k)$ per ogni punto;
3.  **combinazione con il prior:** si moltiplica il valore della verosimiglianza per il corrispondente valore della distribuzione a priori, ottenendo una quantità proporzionale alla densità a posteriori non normalizzata, $u(\theta_k)=p(\theta_k)\cdot L(\theta_k)$;
4.  **normalizzazione:** si divide ciascun valore per la somma totale, trasformando i risultati in una legittima distribuzione di probabilità, $p(\theta_k\mid y)=\dfrac{u(\theta_k)}{\sum_j u(\theta_j)}$.

Il risultato finale, $p(\theta_k\mid y)$, è una rappresentazione discreta della distribuzione a posteriori che mostra come l'evidenza empirica modifichi le credenze iniziali.

### Esempio guidato: aggiornamento bayesiano con una distribuzione a priori discreta

Si consideri uno scenario sperimentale in cui, su un campione di $n$ = 30 soggetti, si osservano $y$ = 22 risposte di "successo". 

#### Definizione della griglia per il parametro `theta`

Il parametro di interesse $\theta$, rappresenta la probabilità di successo ed è per natura continuo e definito nell'intervallo [0, 1]. Ai fini didattici, procediamo a una sua discretizzazione, suddividendo l'intervallo in una griglia di 11 valori equidistanti.

```{r}
theta_griglia <- seq(0, 1, by = 0.1)  
theta_griglia
```

#### Specificazione della distribuzione a priori

In un approccio bayesiano, la definizione della distribuzione a priori rappresenta il primo passo dell'inferenza. Se si adottasse un prior uniforme non informativo, a ciascun valore della griglia verrebbe assegnata la stessa probabilità.

```{r}
prior_uniforme <- rep(1 / length(theta_griglia), length(theta_griglia))
prior_uniforme
```

Al fine di esemplificare un caso più generale e realistico, si è scelto di utilizzare una distribuzione a priori informativa, che attribuisce maggiore plausibilità ai valori centrali del parametro.

```{r}
prior_informativo <- c(0, 0.05, 0.05, 0.05, 0.175, 0.175, 0.175, 0.175, 0.05, 0.05, 0.05)
prior_informativo
```

La configurazione della distribuzione a priori informativa può essere visualizzata graficamente nel modo seguente.

```{r}
ggplot(data.frame(theta = theta_griglia, probabilita = prior_informativo), 
       aes(x = theta, y = probabilita)) +
  geom_col(width = 0.08, fill = PRIMARY, alpha = 0.7) +
  labs(x = expression(theta), y = "Densità di probabilità",
       title = "Prior uniforme discreto") +
  scale_x_continuous(breaks = theta_griglia) +
  theme_minimal()
```

#### Calcolo della funzione di verosimiglianza

La componente empirica dell'inferenza è data dalla funzione di verosimiglianza. Nel caso di dati binomiali, essa quantifica la probabilità di osservare $y$ = 22 successi in $n$ = 30 prove, condizionatamente a ciascun valore possibile di $\theta$.

```{r}
y <- 22
n <- 30
verosimiglianza <- dbinom(y, size = n, prob = theta_griglia)
verosimiglianza
```

#### Aggiornamento bayesiano e calcolo della distribuzione a posteriori 

Il nucleo del teorema di Bayes risiede nella combinazione delle informazioni a priori con l'evidenza fornita dai dati. La distribuzione a posteriori non normalizzata si ottiene moltiplicando, punto per punto, la verosimiglianza per la distribuzione a priori.

```{r}
posteriori_non_norm <- prior_informativo * verosimiglianza
posteriori_non_norm
```

Per ottenere una distribuzione di probabilità corretta, è necessario normalizzare questi valori, assicurandosi che la loro somma sia unitaria.

```{r}
posteriori <- posteriori_non_norm / sum(posteriori_non_norm)
posteriori
```

La proprietà di normalizzazione viene confermata dal calcolo seguente.

```{r}
sum(posteriori)
```

### Statistiche descrittive della distribuzione a posteriori

Una volta ottenuta la distribuzione a posteriori, è possibile sintetizzarne le caratteristiche attraverso le principali statistiche descrittive.

La media posteriori, calcolata come valore atteso, è data da: $\mathbb{E}(\theta | y) = \sum \theta_i \cdot p(\theta_i | y)$.

```{r}
media_posteriori <- sum(theta_griglia * posteriori)
media_posteriori
```

La varianza posteriori è data da: $\mathbb{V}(\theta | y) = \sum (\theta_i - \mathbb{E}(\theta))^2 \cdot p(\theta_i | y)$.

```{r}
varianza_posteriori <- sum(theta_griglia^2 * posteriori) - media_posteriori^2
varianza_posteriori
```

La moda a posteriori corrisponde al valore di $\theta$ con massima probabilità a posteriori.

```{r}
moda_posteriori <- theta_griglia[which.max(posteriori)]
moda_posteriori
```

#### Analisi comparativa e interpretazione dei risultati

La visualizzazione congiunta delle tre distribuzioni – a priori, verosimiglianza e a posteriori – consente di apprezzare visivamente il processo di aggiornamento bayesiano.

```{r}
# Preparazione dei dati per la visualizzazione
dati_plot <- data.frame(
  theta = theta_griglia,
  Prior = prior_informativo,
  Verosimiglianza = verosimiglianza / max(verosimiglianza),  # Scalata per comparazione
  Posteriori = posteriori
)

# Visualizzazione comparativa
ggplot(dati_plot, aes(x = theta)) +
  # Prior come area semitrasparente
  geom_col(aes(y = Prior), fill = modern_palette$grey5, alpha = 0.7, width = 0.08) +
  # Verosimiglianza come linea prominente
  geom_line(aes(y = Verosimiglianza), color = modern_palette$red, linewidth = 1.2) +
  # Posteriori come area colorata
  geom_col(aes(y = Posteriori), fill = PRIMARY, alpha = 0.7, width = 0.08) +
  # Personalizzazione degli assi e del tema
  labs(
    x = expression(theta), 
    y = "Densità di probabilità",
    title = "Aggiornamento bayesiano: confronto tra distribuzioni",
    subtitle = "Prior (grigio), Verosimiglianza (rosso), Posterior (blu)"
  ) +
  scale_x_continuous(
    breaks = theta_griglia,
    expand = expansion(mult = 0.02)
  ) +
  scale_y_continuous(expand = expansion(mult = c(0, 0.05))) +
  theme(
    panel.grid.minor = element_blank(),
    legend.position = "none",
    plot.title = element_text(face = "bold"),
    plot.subtitle = element_text(color = "gray40")
  ) +
  theme_minimal()
```

L'analisi del grafico rivela come l'evidenza empirica, rappresentata dalla funzione di verosimiglianza, interagisca con le convinzioni a priori. La verosimiglianza presenta un picco marcato nell'intervallo 0.7–0.8, indicando che i valori di $\theta$ compresi in tale intervallo sono i più compatibili con i dati osservati. La distribuzione a posteriori, sintesi dell'informazione precedente e di quella campionaria, risulta concentrata attorno a $\theta \approx 0.7$, dimostrando che l'aggiornamento bayesiano permette di affinare le stime iniziali alla luce dell'evidenza empirica.

In conclusione, questo esempio guidato mostra come il paradigma bayesiano fornisca un quadro coerente e trasparente per l'aggiornamento delle conoscenze in presenza di nuovi dati.

### Interpretazione psicologica della distribuzione a posteriori

A questo punto possiamo tornare all’interpretazione del parametro $\theta$ nel contesto dello studio psicologico preso in esame. In questo esempio, $\theta$ rappresenta la probabilità — nella popolazione di riferimento — che un individuo giudichi *moralmente accettabile* l’azione di deviare il treno per salvare cinque persone sacrificandone una.

La distribuzione a posteriori ottenuta con il metodo basato sulla griglia fornisce informazioni molto più dettagliate rispetto a una singola stima puntuale. Non ci fornisce soltanto un numero (“la proporzione stimata è circa 0.73”), ma ci offre un intero spettro di plausibilità che descrive il livello di incertezza su questo valore, alla luce dei dati e delle ipotesi iniziali.

* **Prima dei dati**, la distribuzione a priori rifletteva una convinzione di equilibrio: i valori estremi di $\theta$ (molto bassi o molto alti) erano ritenuti improbabili, coerentemente con l'idea che le persone potessero dividersi in modo abbastanza bilanciato nel giudicare moralmente il dilemma.
* **Dopo l’osservazione dei dati**, la distribuzione a posteriori mostra che la credenza si è spostata in modo deciso verso valori di $\theta$ compresi tra 0.7 e 0.8, indicando che nella popolazione considerata è più probabile che la maggioranza giudichi accettabile l’azione.

L’incertezza residua, rappresentata dalla larghezza della distribuzione a posteriori, ci ricorda però che non possiamo escludere del tutto valori più bassi, ad esempio intorno a 0.6, specialmente se il campione è modesto.

In termini psicologici, la distribuzione a posteriori quantifica dunque la forza dell'evidenza empirica in favore dell'interpretazione secondo cui la norma morale utilitaristica (sacrificare uno per salvare cinque) gode di ampia accettazione nella popolazione osservata, pur con margini di incertezza che riflettono la variabilità individuale e la dimensione del campione.

Questa rappresentazione probabilistica del risultato consente un tipo di inferenza più ricco rispetto alla sola stima frequentista della proporzione:

* mostra *quanto* siamo fiduciosi che la proporzione superi determinate soglie (ad esempio $P(\theta > 0.5))$;
* consente di *confrontare direttamente* credenze preesistenti e nuove evidenze;
* inoltre, e soprattutto, integra *l’incertezza epistemica* in modo esplicito, rendendo trasparente il grado di fiducia che possiamo riporre nella conclusione.

In questo modo, il metodo basato su griglia, pur nella sua semplicità, traduce efficacemente il principio fondamentale dell’inferenza bayesiana applicato a un caso concreto di psicologia sperimentale:

> le nostre convinzioni iniziali sul comportamento umano vengono aggiornate alla luce dei dati, producendo una rappresentazione quantitativa della conoscenza che incorpora sia ciò che già sapevamo, sia ciò che abbiamo appena osservato.

## Aggiornamento bayesiano con distribuzioni a priori continue

Nella sezione precedente, il processo di aggiornamento bayesiano è stato presentato con l'approccio discretizzato basato su griglia, che approssima il parametro $\theta$ in un insieme finito di valori nell'intervallo [0, 1]. Ora, abbandoniamo questa schematizzazione a favore di una rappresentazione continua dell'incertezza a priori.

A tale scopo, useremo la distribuzione Beta per rappresentare le nostre conoscenze a priori sotto forma di una distribuzione continua. La distribuzione Beta, infatti, è definita proprio sull'intervallo [0, 1], il che la rende perfettamente compatibile con il dominio di $\theta$. Inoltre, gode della proprietà di essere coniugata alla verosimiglianza binomiale. Questa proprietà garantisce che la distribuzione a posteriori appartenga alla stessa famiglia di quella a priori, semplificando notevolmente il calcolo. Di conseguenza, sarà possibile derivare la distribuzione a posteriori in forma analitica chiusa, calcolare statistiche riassuntive in modo esatto senza approssimazioni numeriche e comprendere in modo trasparente come l'informazione iniziale si fonde con l'evidenza fornita dai nuovi dati.

### Il modello Beta-Binomiale: l'aggiornamento in forma chiusa

Si consideri il modello statistico bayesiano
$$
\theta \sim \mathrm{Beta}(\alpha,\,\beta), \qquad Y \mid \theta \sim \mathrm{Binomiale}(n,\,\theta),
$$
dove $Y = y$ è il dato campionario osservato. Sfruttando la proprietà di coniugazione, la distribuzione a posteriori di $\theta$ si esprime analiticamente come:
$$
\theta \mid y \;\sim\; \mathrm{Beta}(\alpha + y,\, \beta + n - y).
$$

#### Interpretazione mediante pseudo-osservazioni

Il meccanismo di aggiornamento bayesiano ammette un'interpretazione intuitiva in termini di *conteggi equivalenti*:

* $\alpha$ corrisponde al numero di *successi a priori*,
* $\beta$ corrisponde al numero di *insuccessi a priori*.

L'aggiornamento si riduce quindi a una semplice operazione di aggregazione:

* i successi osservati $y$ si sommano ad $\alpha$,
* gli insuccessi osservati $n-y$ si sommano a $\beta$.

Questa interpretazione fornisce un ponte concettuale tra la conoscenza a priori e l'evidenza empirica: partiamo da un'"esperienza virtuale" codificata nel prior e la integriamo con l'esperienza reale rappresentata dai dati.

#### Parametrizzazione alternativa: media e concentrazione

Per una specificazione più intuitiva del prior, è utile la seguente riparametrizzazione:

* *media a priori*, $m=\dfrac{\alpha}{\alpha+\beta}$,
* *concentrazione*, $\kappa=\alpha+\beta$ (quanto il prior è “sicuro”: più grande $\kappa$, più è concentrato).

Il parametro $\kappa$ quantifica la "forza" del prior: valori elevati indicano una maggiore certezza nella stima a priori $m$. La varianza del prior,
$$
\operatorname{Var}(\theta) = \frac{m(1-m)}{\kappa+1},
$$
decresce al crescere di $\kappa$, confermando l'interpretazione di $\kappa$ come *dimensione campionaria effettiva* del prior.

Il valore atteso a posteriori è una media ponderata tra la credenza a priori e l'evidenza empirica:
$$
\mathbb{E}[\theta \mid y] = \underbrace{\frac{\kappa}{\kappa + n}}_{\text{peso del prior}} \cdot m \;+\; \underbrace{\frac{n}{\kappa + n}}_{\text{peso dei dati}} \cdot \frac{y}{n}.
$$

Questa formulazione rende esplicito il trade-off bayesiano: quando $n \gg \kappa$, l'evidenza empirica prevale; quando $\kappa \gg n$, il prior stabilizza la stima, evitando conclusioni estreme basate su campioni piccoli.

#### Un esempio numerico: dall'a priori debole alla posterior informata

Supponiamo di considerare un esperimento con 30 prove che produce 22 successi, corrispondenti a una proporzione campionaria pari a 22/30, ovvero 0.733.

Specifichiamo come distribuzione a priori una distribuzione $\mathrm{Beta}(2,2)$, caratterizzata da:

- media a priori: $m = 2/(2+2) = 0.5$,
- concentrazione: $\kappa = 4$ (prior debole, equivalente a 4 osservazioni virtuali).

Applicando il teorema di Bayes in forma chiusa si ottiene la distribuzione a posteriori:
$$
\theta \mid y \sim \mathrm{Beta}(2 + 22,\, 2 + 30 - 22) = \mathrm{Beta}(24, 10).
$$

La media a posteriori risulta:
$$
\mathbb{E}[\theta \mid y] = \frac{24}{24 + 10} \approx 0.706.
$$

**Interpretazione del risultato**: nonostante la proporzione campionaria sia $0.733$, la stima bayesiana ($0.706$) risulta più conservativa. Questo effetto di "reggressione verso la media a priori" illustra il principio di *shrinkage bayesiano*: il prior, seppur debole, esercita un effetto stabilizzante, particolarmente utile quando la dimensione del campione è limitata.

La media a posteriori può essere espressa come media ponderata:
$$
0.706 = \underbrace{\frac{4}{4 + 30}}_{\text{15%}} \times 0.5 + \underbrace{\frac{30}{4 + 30}}_{\text{85%}} \times 0.733,
$$
dove i pesi riflettono l'informazione relativa del prior (15%) rispetto ai dati osservati (85%).

#### Evoluzione dell'incertezza a posteriori

La varianza a posteriori della distribuzione Beta è data da:
$$
\mathbb{V}[\theta \mid y] = \frac{(\alpha + y)(\beta + n - y)}{(\alpha + \beta + n)^2(\alpha + \beta + n + 1)} = \frac{24 \times 10}{34^2 \times 35} \approx 0.0059.
$$

Questo risultato rappresenta una rilevante riduzione rispetto alla varianza a priori, pari a $\mathbb{V}[\theta] = \frac{2 \times 2}{4^2 \times 5} = 0.05$. Il processo di aggiornamento bayesiano determina quindi una contrazione dell'incertezza: all'aumentare della dimensione del campione $n$ o della concentrazione del prior, la distribuzione a posteriori diventa più stretta, riflettendo una maggiore precisione nella stima del parametro.

#### Relazione con il metodo a griglia

Il metodo a griglia, presentato nella sezione precedente, costituisce un'approssimazione numerica della soluzione analitica. Se si discretizzasse l'intervallo $[0,1]$ con una griglia sufficientemente fine e si calcolasse la posteriori come prodotto del prior $\mathrm{Beta}(\alpha,\beta)$ e della verosimiglianza binomiale $\mathrm{Bin}(y \mid n,\theta)$ - seguito da normalizzazione numerica - il risultato convergerebbe alla soluzione esatta $\mathrm{Beta}(\alpha + y, \beta + n - y)$ man mano che la griglia si affina.

Questa corrispondenza conferma la coerenza dei due approcci: il metodo a griglia rappresenta una generalizzazione computazionale che mantiene la sua validità anche in contesti non coniugati, mentre la forma chiusa Beta-binomiale offre efficienza computazionale e trasparenza interpretativa quando le condizioni di coniugazione sono soddisfatte.

### Gestione del conflitto tra credenze a priori e dati

In alcune applicazioni può emergere una tensione significativa tra le credenze a priori e l'evidenza empirica. Una diagnosi operativa consiste nel valutare la posizione del dato osservato $y$ all'interno della *distribuzione predittiva a priori* $Y \sim \mathrm{Beta\text{-}Binomiale}(n,\alpha,\beta)$. Se $y$ cade oltre il 97.5° percentile (o sotto il 2.5° percentile), ciò suggerisce un potenziale conflitto tra i dati e le credenze a priori.

In tali circostanze, è consigliabile:

- *rivalutare la media a priori* $m$ alla luce di nuove conoscenze contestuali;
- *ridurre la concentrazione* $\kappa$ del prior, attenuandone il peso informativo;
- considerare l'adozione di *prior debolmente informativi* che esercitino una regolarizzazione meno stringente.

### Tipologie di prior: caratteristiche e applicazioni

* **Prior uniforme** $\mathrm{Beta}(1,1)$. Assegna uguale densità a tutti i valori di $\theta$, rappresentando uno stato di massima ignoranza iniziale. Tuttavia, può risultare eccessivamente permissivo in piccoli campioni.

* **Prior di Jeffreys** $\mathrm{Beta}(1/2,1/2)$. Questo prior garantisce l'invarianza rispetto alle riparametrizzazioni e costituisce una scelta di riferimento in contesti fondazionali. Presenta una maggiore massa probabilistica nelle code rispetto al prior uniforme.

* **Prior debolmente informativi** (es. $\mathrm{Beta}(2,2)$): offrono un bilanciamento ottimale tra regolarizzazione e flessibilità, evitando stime estreme senza imporre convinzioni a priori troppo rigide. Sono particolarmente indicati in ambito didattico e applicativo.

* **Prior informativi**: derivati da evidenze preesistenti (meta-analisi, letteratura scientifica), specificano sia la localizzazione $m$ che l'incertezza $\kappa$ sulla base di conoscenze consolidate.

### Sintesi del framework Beta-Binomiale

Il modello Beta-Binomiale costituisce una *palestra concettuale* ideale per comprendere i meccanismi fondamentali dell'inferenza bayesiana:

- l'aggiornamento si realizza attraverso una semplice aggregazione di conteggi: $\alpha \rightarrow \alpha + y$, $\beta \rightarrow \beta + n - y$;
- la stima a posteriori sintetizza in modo trasparente le credenze iniziali e l'evidenza empirica attraverso una media ponderata;
- l'incertezza si contrae progressivamente con l'aumentare dei dati, con la varianza posteriore che diminuisce con l'aumentare di $n$;
- la flessibilità esplicativa del prior permette di adattare il modello a diversi contesti applicativi.

Queste proprietà rendono il caso Beta-Binomiale un *componente fondamentale* sia per la didattica dell'inferenza bayesiana, sia come elemento costitutivo di modelli gerarchici più complessi.

::: {.callout-note collapse="true"}
## Approfondimento — Metodo su griglia con la Normale (media ignota, $\sigma$ nota)

Il metodo su griglia si estende efficacemente oltre i modelli binomiali, applicandosi anche al caso di dati normalmente distribuiti con varianza nota. L’idea è la stessa: definiamo una griglia di valori plausibili per $\mu$, calcoliamo la verosimiglianza, la combiniamo con la *prior* e normalizziamo per ottenere la distribuzione a posteriori.

### Scenario applicativo

Consideriamo lo studio dei punteggi di QI in un campione di bambini ad alto potenziale. Assumiamo che i punteggi seguano una distribuzione Normale con deviazione standard nota $\sigma = 5$ nota, mentre la media $\mu$ il parametro da stimare. Specifichiamo un prior informativo:

$$
\mu \sim \mathcal{N}(140, 3^2).
$$

```{r}
set.seed(123)
campione <- round(rnorm(10, mean = 130, sd = 5))  # Simulazione di 10 osservazioni
campione
sigma <- 5  # Deviazione standard nota
```

### Fondamenti teorici

La verosimiglianza congiunta per $n$ osservazioni indipendenti è data da:

$$
L(\mu) = \prod_{i=1}^n \text{Normal}(y_i \mid \mu, \sigma).
$$

Per garantire stabilità numerica, è conveniente lavorare nello spazio logaritmico, trasformando il prodotto in una somma.

### Implementazione numerica stabile

```{r}
# Definizione della griglia per il parametro μ
mu_griglia <- seq(120, 150, length.out = 400)

# Calcolo della log-verosimiglianza
log_lik <- sapply(mu_griglia, function(mu)
  sum(dnorm(campione, mean = mu, sd = sigma, log = TRUE))
)

# Calcolo della log-densità a priori
log_prior <- dnorm(mu_griglia, mean = 140, sd = 3, log = TRUE)

# Combinazione per ottenere la log-posterior non normalizzata
log_post_unnorm <- log_lik + log_prior

# Normalizzazione mediante tecnica log-sum-exp
log_post_stab <- log_post_unnorm - max(log_post_unnorm)
post <- exp(log_post_stab)
post <- post / sum(post)

# Calcolo delle statistiche a posteriori
post_mean <- sum(mu_griglia * post)
post_var <- sum((mu_griglia - post_mean)^2 * post)
post_sd <- sqrt(post_var)

c(media_post = post_mean, sd_post = post_sd)
```

### Visualizzazione comparativa

Per un confronto visivo efficace, normalizziamo le distribuzioni alla stessa scala:

```{r}
# Scaling per visualizzazione comparativa
prior_vis <- dnorm(mu_griglia, mean = 140, sd = 3)
prior_vis <- prior_vis / max(prior_vis)

lik_vis <- exp(log_lik - max(log_lik))

df <- tibble(
  mu = mu_griglia,
  Prior = prior_vis,
  Verosimiglianza = lik_vis,
  Posterior = post / max(post)
)

df |>
  pivot_longer(-mu, names_to = "Distribuzione", values_to = "Densita") |>
  ggplot(aes(x = mu, y = Densita, color = Distribuzione)) +
  geom_line(linewidth = 1.1) +
  geom_vline(xintercept = mean(campione), linetype = "dashed", 
             color = "red", alpha = 0.7) +
  geom_vline(xintercept = 140, linetype = "dotted", 
             color = "blue", alpha = 0.7) +
  geom_vline(xintercept = post_mean, linetype = "dotdash", 
             color = "darkgreen", alpha = 0.7) +
  labs(
    x = expression(mu), 
    y = "Densità (scala comparativa)",
    title = "Aggiornamento bayesiano: modello Normale"
  ) +
  theme(legend.position = "bottom") +
  theme_minimal()
```

### Interpretazione del grafico

- **Verosimiglianza**: Centrata sulla media campionaria, indica i valori di μ più compatibili con i dati osservati
- **Prior**: Centrato a 140, rappresenta le credenze iniziali del ricercatore
- **Posterior**: Sintetizza l'aggiornamento bayesiano, bilanciando evidenza empirica e conoscenza a priori

All'aumentare della dimensione campionaria, la distribuzione a posteriori tende a concentrarsi progressivamente attorno alla media empirica.

### Verifica analitica mediante coniugazione

Nel caso Normale-Normale con varianza nota, la distribuzione a posteriori ammette forma chiusa:

$$
\mu \mid y \sim \mathcal{N}(\mu_\text{post}, \tau_\text{post}^2),
$$

dove:

$$
\tau_\text{post}^{-2} = \frac{n}{\sigma^2} + \tau_0^{-2},
\quad
\mu_\text{post} = \tau_\text{post}^2 \left(\frac{n\bar{y}}{\sigma^2} + \frac{\mu_0}{\tau_0^2}\right).
$$

```{r}
n <- length(campione)
ybar <- mean(campione)
mu0 <- 140
tau0 <- 3

tau2_post <- 1 / (n / sigma^2 + 1 / tau0^2)
mu_post <- tau2_post * (n * ybar / sigma^2 + mu0 / tau0^2)
sd_post <- sqrt(tau2_post)

c(analitico_media = mu_post, analitico_sd = sd_post,
  griglia_media = post_mean, griglia_sd = post_sd)
```

La concordanza tra i risultati conferma la correttezza dell'implementazione numerica.

### Considerazioni sulla stabilità numerica

La verosimiglianza congiunta, essendo un prodotto di densità di probabilità, può assumere valori numericamente prossimi allo zero. L'utilizzo sistematico dei logaritmi, unitamente alla tecnica di normalizzazione log-sum-exp, costituisce una pratica essenziale per prevenire problemi di underflow in qualsiasi implementazione bayesiana pratica.
:::

## Riflessioni conclusive {.unnumbered .unlisted}

In questo capitolo abbiamo esaminato il caso fondamentale dell'inferenza bayesiana per una proporzione binomiale. L'integrazione tra la verosimiglianza binomiale e la distribuzione a priori Beta ha mostrato il teorema di Bayes nella sua forma più essenziale: un processo di sintesi tra conoscenza pregressa ed evidenza empirica che produce una distribuzione a posteriori coerente e informativa.

Questo esempio apparentemente semplice rivela un principio metodologico profondo: l'incertezza non è un ostacolo da eliminare, ma una dimensione costitutiva del ragionamento scientifico. A differenza degli approcci che riducono l'inferenza a stime puntuali o a test dicotomici, il framework bayesiano preserva la ricchezza informativa del processo inferenziale offrendo una rappresentazione trasparente e graduale dell'evoluzione delle conoscenze.

Sul piano computazionale, il metodo a griglia si è rivelato uno strumento didattico di eccezionale valore euristico. La sua logica trasparente, basata sulla discretizzazione dello spazio parametrico, sulla valutazione di prior e verosimiglianza e sulla normalizzazione, rende tangibile il meccanismo dell'aggiornamento bayesiano. Tuttavia, la sua applicabilità pratica è limitata dalla cosiddetta "maledizione della dimensionalità"[^maledizione]: all'aumentare del numero di parametri, la complessità computazionale cresce esponenzialmente, rendendo necessari approcci alternativi.

[^maledizione]: La dimensionalità dello spazio dei parametri cresce in modo esponenziale al crescere della complessità del modello. Ad esempio, discretizzando ciascun parametro in 100 valori possibili, un modello con 10 parametri richiederebbe $100^{10} = 10^{20}$ valutazioni della distribuzione a posteriori, una quantità computazionalmente intrattabile. Questo fenomeno, noto come *maledizione della dimensionalità*, rendendo necessari metodi approssimati come il campionamento Monte Carlo.

Proprio per superare queste limitazioni, nei prossimi capitoli esploreremo metodologie più scalabili, come il *campionamento a catena di Markov* (MCMC). Sebbene siano tecnicamente più sofisticate, queste tecniche preservano la logica bayesiana fondamentale che abbiamo osservato nel caso del modello Beta-binomiale. Il metodo a griglia mantiene quindi un grande valore come strumento pedagogico, fornendo le basi concettuali per affrontare modelli più complessi senza perdere di vista i principi fondamentali dell'inferenza bayesiana.

::: {.callout-note}

#### Messaggio chiave

L'analisi di casi semplici, come la stima di una proporzione, permette di apprezzare l'essenza dell'inferenza bayesiana: non la ricerca di un valore definitivo, ma la caratterizzazione di uno spettro di plausibilità e dell'incertezza associata. Nei capitoli successivi estenderemo questa logica a modelli progressivamente più complessi, fino a descrivere i *processi psicologici* che generano i dati osservati.
:::

::: {.callout-important title="Esercizio" collapse="true"}

In uno studio sulla percezione delle emozioni, un partecipante osserva 10 fotografie di volti arrabbiati. Deve indicare se il volto esprime **rabbia** o no. Ogni risposta può essere **corretta (1)** o **errata (0)**.

I dati osservati del partecipante sono:

```
1, 0, 1, 1, 1, 0, 0, 1, 1, 1
```

→ Totale: **7 successi** su **10 prove** → $y = 7$, $n = 10$.

**Obiettivo:** stimare la probabilità $\theta$ che il partecipante riconosca correttamente un volto arrabbiato, tenendo conto sia dei **dati osservati** sia di **conoscenze pregresse**.

**Prior Informativo.**

Supponiamo di voler adottare un approccio cautamente *pessimistico* sulle capacità iniziali del partecipante, basandoci su studi precedenti che indicano un riconoscimento della rabbia **non sempre accurato**, ad esempio mediamente intorno al **40%** con moderata incertezza.

Per rappresentare questa convinzione, scegliamo come **distribuzione a priori** una **Beta(4, 6)**:

- **Media**: $\mu = \frac{4}{4+6} = 0.4$
- **Varianza**: $\frac{4 \cdot 6}{(10)^2 \cdot 11} = 0.0218$

Questa prior concentra la massa di probabilità su valori inferiori a 0.5, ma lascia spazio anche a livelli di competenza superiori.


**Calcolo della Distribuzione a Posteriori con il Metodo Basato su Griglia.**

**1. Griglia di valori per $\theta$.**

```{r}
theta <- seq(0, 1, length.out = 1000)
head(theta)
tail(theta)
```

**2. Calcolo della distribuzione a priori Beta(4, 6).**

```{r}
prior <- dbeta(theta, shape1 = 4, shape2 = 6)
prior <- prior / sum(prior)  # normalizzazione
```

Visualizzazione:

```{r}
ggplot(data.frame(theta, prior), aes(x = theta, y = prior)) +
  geom_line(linewidth = 1.2) +
  labs(
    x = expression(theta),
    y = "Densità (normalizzata)"
  ) +
  theme_minimal(base_size = 14)
```

**3. Calcolo della verosimiglianza per 7 successi su 10.**

```{r}
likelihood <- dbinom(7, size = 10, prob = theta)
likelihood <- likelihood / sum(likelihood)  # normalizzazione
```

Visualizzazione:

```{r}
ggplot(data.frame(theta, likelihood), aes(x = theta, y = likelihood)) +
  geom_line(linewidth = 1.2) +
  labs(
    x = expression(theta),
    y = "Densità (normalizzata)"
  ) +
  theme_minimal(base_size = 14)
```

**4. Calcolo della distribuzione a posteriori.**

```{r}
unnormalized_posterior <- prior * likelihood
posterior <- unnormalized_posterior / sum(unnormalized_posterior)
```

**5. Visualizzazione congiunta: prior, likelihood e posteriori.**

```{r}
data <- data.frame(theta, prior, likelihood, posterior)

# Imposta i livelli desiderati con nomi leggibili
long_data <- pivot_longer(
  data,
  cols = c("prior", "likelihood", "posterior"),
  names_to = "distribution",
  values_to = "density"
) |>
  mutate(distribution = factor(
    distribution,
    levels = c("prior", "likelihood", "posterior"),
    labels = c("A Priori", "Verosimiglianza", "A Posteriori")
    )
  )

ggplot(
  long_data, 
  aes(x = theta, y = density, color = distribution)
  ) +
  geom_line(linewidth = 1.2) +
  labs(
    x = expression(theta),
    y = "Densità (normalizzata)",
    color = NULL
  ) +
  theme(legend.position = "bottom")
```

**Riepilogo:**

- la **prior** (Beta(4,6)) riflette una convinzione iniziale più scettica;
- la **verosimiglianza** è centrata su $\theta = 0.7$, corrispondente a 7 successi su 10;
- la **posteriori** media tra prior e dati, ma si sposta chiaramente verso destra, evidenziando l’**effetto aggiornamento bayesiano**.

Questo esempio mostra come l’approccio bayesiano:

- **integra in modo trasparente** dati individuali e credenze pregresse;
- **produce una stima personalizzata** della capacità del partecipante;
- permette di **quantificare l’incertezza** in modo completo, tramite la distribuzione a posteriori.

**Quantità a Posteriori.**

Media:

```{r}
posterior_mean <- sum(theta * posterior)
posterior_mean
```

Deviazione standard:

```{r}
posterior_sd <- sqrt(sum((theta^2) * posterior) - posterior_mean^2)
posterior_sd
```

Moda:

```{r}
posterior_mode <- theta[which.max(posterior)]
posterior_mode
```

Intervallo di credibilità al 94%:

```{r}
samples <- sample(theta, size = 10000, replace = TRUE, prob = posterior)
quantile(samples, probs = c(0.03, 0.97))
```

Questo esercizio mostra come:

- l’informazione pregressa può essere incorporata in modo trasparente in un modello bayesiano;
- la posteriori riflette **una combinazione tra dati osservati e conoscenze precedenti**.

:::

::: {.callout-important title="Problemi 1" collapse="true"}
In uno studio sull'analisi delle pratiche di trasparenza e riproducibilità nella ricerca in psicologia, @hardwicke2022estimating hanno riportato che la condivisione dei materiali di ricerca è stata rilevata nel 14% dei casi (26 su 183 studi), con un intervallo di confidenza al 95% pari a [10%, 19%]. Questo suggerisce che la condivisione di materiali è rara.

Ispirandoti ai risultati di questo studio, costruisci una distribuzione a priori per la probabilità $\theta$ che uno studio condivida i materiali di ricerca. Per semplicità, discretizza $\theta$ in 10 livelli equispaziati: $0.05, 0.15, 0.25, 0.35, 0.45, 0.55, 0.65, 0.75, 0.85, 0.95$.

Attribuisci le seguenti probabilità a priori ai 10 livelli, basandoti sull'informazione che la condivisione dei materiali è un evento raro ma non trascurabile: $0.05, 0.20, 0.30, 0.15, 0.10, 0.08, 0.05, 0.03, 0.02, 0.02$.

Supponiamo che siano stati osservati 20 studi su 100 che hanno condiviso i materiali di ricerca. Calcola la distribuzione a posteriori utilizzando il metodo basato su griglia. Calcola la media della distribuzione a posteriori e l'intervallo di credibilità al 89%.
:::

::: {.callout-tip title="Soluzioni 1" collapse="true"}
```r
# Definizione dei possibili valori di theta (probabilità discreta)
theta <- seq(0.05, 0.95, by = 0.10)

# Definizione della distribuzione a priori
prior <- c(0.05, 0.20, 0.30, 0.15, 0.10, 0.08, 0.05, 0.03, 0.02, 0.02)

# Normalizzazione della prior (se necessario, ma in questo caso già normalizzata)
prior <- prior / sum(prior)

# Dati osservati
successi <- 20  # studi che hanno condiviso materiali
n <- 100        # studi totali

# Calcolo della verosimiglianza usando la distribuzione binomiale
likelihood <- dbinom(successi, size = n, prob = theta)

# Calcolo della distribuzione a posteriori (applicazione del teorema di Bayes)
posterior <- likelihood * prior
posterior <- posterior / sum(posterior)  # Normalizzazione

# Calcolo della media della distribuzione a posteriori
posterior_mean <- sum(theta * posterior)

# Calcolo dell'intervallo di credibilità al 89%
cdf <- cumsum(posterior)  # Distribuzione cumulativa
lower_bound <- theta[which.min(abs(cdf - 0.055))]  # 5.5% quantile
upper_bound <- theta[which.min(abs(cdf - 0.945))]  # 94.5% quantile

# Output dei risultati
list(
  posterior_mean = posterior_mean,
  credibility_interval_89 = c(lower_bound, upper_bound)
)
```
:::

::: {.callout-important title="Problemi 2" collapse="true"}

L'obiettivo di questo esercizio è applicare il metodo basato su griglia per stimare la distribuzione a posteriori di una proporzione, utilizzando dati dalla Scala della Rete Sociale di Lubben (LSNS-6). Si assume che un punteggio LSNS-6 superiore a una soglia prefissata indichi isolamento sociale. Il compito è:

1. Scegliere una soglia per classificare i partecipanti in due gruppi (isolati vs. non isolati), garantendo che la proporzione osservata non sia inferiore a 0.1 o superiore a 0.9.
2. Calcolare la distribuzione a posteriori della proporzione usando un'approssimazione discreta su una griglia di valori.
3. Determinare l'intervallo di credibilità all'89%.
4. Interpretare i risultati.

**Consegna:**
caricare su Moodle il file .qmd compilato in pdf.
:::

::: {.callout-tip title="Soluzioni 2" collapse="true"}
**Dati e Modellizzazione**

Si assume che i dati siano rappresentati da una variabile binaria $y$, con $y = 1$ per individui classificati come isolati e $y = 0$ altrimenti. Supponiamo che su un campione di $n$ individui, $s$ siano isolati.

Definiamo il modello statistico:

$$ y_i \sim \text{Bernoulli}(\theta) $$

con:

- $y_i \in \{0,1\}$ per $i=1,\dots,n$,
- $\theta$ proporzione di individui isolati nella popolazione.

La distribuzione a priori su $\theta$ è scelta come $\text{Beta}(2,2)$, che rappresenta una conoscenza iniziale moderata e non estrema.

**Metodo basato su griglia**

Il metodo a griglia approssima la distribuzione a posteriori calcolando la probabilità per una serie di valori discreti di $\theta$.

1. **Definire una griglia di valori per $\theta$**:

    ```r
    theta <- seq(0, 1, length.out = 100)
    ```
2. **Calcolare la distribuzione a priori**:

    ```r
    prior <- dbeta(theta, 2, 2)
    prior <- prior / sum(prior)  # Normalizzazione
    ```
3. **Calcolare la verosimiglianza**:

    ```r
    likelihood <- dbinom(s, size = n, prob = theta)
    likelihood <- likelihood / sum(likelihood)  # Normalizzazione
    ```
4. **Calcolare la distribuzione a posteriori**:

    ```r
    posterior <- prior * likelihood
    posterior <- posterior / sum(posterior)  # Normalizzazione
    ```

** Calcolo dell'intervallo di credibilità all'89%**

L'intervallo di credibilità è calcolato come l'intervallo che contiene il 89% della probabilità a posteriori.

```r
ci_89 <- quantile(sample(theta, size = 10000, prob = posterior, replace = TRUE), probs = c(0.055, 0.945))
ci_89
```

**Interpretazione dei risultati**

1. **Valore atteso e moda a posteriori**:

    ```r
    mean_theta <- sum(theta * posterior)
    mode_theta <- theta[which.max(posterior)]
    ```
    
    - Il valore atteso fornisce una stima puntuale di $\theta$.
    - La moda indica il valore più probabile della proporzione di isolamento sociale.

2. **Intervallo di credibilità**:

    - L'89% della probabilità a posteriori cade tra i valori dell'intervallo di credibilità.
    - Se l'intervallo è stretto, c'è maggiore certezza sulla proporzione stimata.
    - Se l'intervallo è ampio, vi è maggiore incertezza sulla proporzione.
:::

::: {.callout-note collapse=true title="Informazioni sull'ambiente di sviluppo"}
```{r}
sessionInfo()
```
:::

## Bibliografia {.unnumbered .unlisted}



