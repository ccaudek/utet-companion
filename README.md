# File finali per utet-companion

Il Companion usa lo stesso sistema comune degli altri tre siti, con una variante dedicata.

Copiare:

- `index.qmd`
- `_quarto.yml`
- `style/companion.scss`

Se il progetto non contiene ancora le versioni comuni aggiornate, copiare anche:

- `style/custom.scss`
- `style/custom-dark.scss`

Il vecchio `style/cards.css` non è più richiamato dallo YAML proposto. Può restare nel repository, ma non viene caricato.
